name: Building GSI..

on:
  watch:
    types: [started]
    
env:
  ROM_URL:https://doc-08-8c-docs.googleusercontent.com/docs/securesc/ih4f651hpellbqtffun0eh64u395qvs9/8hr7703mldgq6578a1v1iob7577cbvgl/1606650525000/05847928325444896811/12873326971018064583Z/1h-vwHC-U55KSn9RyYiUOGMKDyfXEdc-K?e=download&nonce=06gm01qrifno8&user=12873326971018064583Z&hash=bpn80hi4qkqsma17ict92

  ROM_NAME: PixelExperince
  TZ: Asia/Kolkata
  MIR: wet
  ZIP_NAME: Flyme-8
jobs:
  build:
    runs-on: ubuntu-latest


    steps:
       - name: Checkout
         uses: actions/checkout@master
           
       - name: Initializing environment
         run: |
            sudo -E apt-get -qq update
            sudo -E apt-get -qq install git openjdk-8-jdk wget

       
       - name: Clone ErfanGSI Source Code
         run: git clone --recurse-submodules https://github.com/ypirnaz9/ErfanGSIs-VelanGSIs.git
       
       - name: Setting up ErfanGSI requirements
         run: |
              sudo chmod -R 777 ErfanGSIs
              cd ErfanGSIs
              sudo bash setup.sh
       
       - name: Download Stock Rom & Generate GSI
         run: sudo ./ErfanGSIs/url2GSI.sh $ROM_URL $ROM_NAME
       
       - name: Zip Aonly and upload 
         run: |
              mkdir final
              sudo chmod -R 777 ErfanGSIs/output
              zip -r ErfanGSIs/output/$ZIP_NAME-GSI-Aonly.7z ErfanGSIs/output/*-Aonly-*.img
              curl -sL https://git.io/file-transfer | sh
              ./transfer $MIR ErfanGSIs/output/$ZIP_NAME-GSI-Aonly.7z          
       - name: Zip AB and upload 
         run: |
              zip -r ErfanGSIs/output/$ZIP_NAME-GSI-AB.7z ErfanGSIs/output/*-AB-*.img
              ./transfer $MIR ErfanGSIs/output/$ZIP_NAME-GSI-AB.7z      
       - name: Print build info
         run: |
              cat ErfanGSIs/output/*-Aonly-*.txt
         
            
